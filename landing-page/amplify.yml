version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build commands required for static HTML/CSS site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []